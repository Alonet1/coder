#include <stdio.h>

FILE *fopen( "/tmp/holo.c" , "r"+ ); // to open a file 
int fclose( FILE "/tmp/holo.c"; // to close a file 
//  an error in closing the file.
DIR //to creat a directory -----> not conformedThe fclose(-) 
int fputc( int c, FILE *10.c ); //The function fputc() writes the character value of the argument c to
// the output stream referenced by fp. It returns the written character written on success otherwise 
//EOF if there iint fputs( const char *s, FILE *fp );
//string to a stream 
int fputs( const char *s, FILE *fp );//The function fputs() writes the string s to the output stream 
//referenced by fp. It returns a non-negative value on success, otherwise EOF
// s returned in case of any error. You can use 
int fprintf(FILE *fp,const char *format, ...)
